jQuery(document).ready(function($) {
	$('.carousel-main').owlCarousel({
	  // Configuración del slider
	  items: 1,
	  loop: true,
	  autoplay: true,
	  autoplayTimeout: 4500,
	  margin: 10,
	  nav:false,
	  dots: false,
	});
  });
